using staj_r_backend.Models;
using staj_r_backend.Models.QueryHandlers;
using staj_r_backend.Models.Queries;
using staj_r_backend.Models.Entities;
using System.Threading.Tasks;
using System.Collections.Generic;

namespace staj_r_backend.Controllers
{
    public class PhoneControllers
    {
        /*
        public async Task<List<Phone>> getPhones()
        {
            
            PhoneHandlers ph  = new PhoneHandlers();
            try
            {
                return await ph.getPhonesHandler(PhoneQueries.getPhonesQueryString, PhoneQueries.getPhonesVariable);
            }
            catch
            {
                return new List<Phone> { new Phone() {brand = "Sonuçlar alınırken hata oluştu."} };
            }
        }
        */
    }
}
